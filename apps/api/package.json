{
  "name": "@aft.page/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "posttest": "node ../cli/check.mjs && node ../plugin/check.mjs",
    "test:watch": "vitest",
    "check:cli": "node ../cli/check.mjs",
    "smoke": "node ./scripts/smoke.mjs",
    "audit": "node ./scripts/audit.mjs",
    "audit:security": "node ./scripts/security-audit.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^4.20250726.0",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.26.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.4",
    "workers-og": "^0.0.27"
  }
}
