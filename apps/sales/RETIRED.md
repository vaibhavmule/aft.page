# Retired

The sales Agents SDK app is retired.

`sales.aft.page` redirects to `https://aft.page/`.

Founder ops HTML + checklist were also retired (8 Sep 2026). Rebuild founder
console in workspace `ops/` and `aft deploy` — see `docs/parked/smoke-audit-ops.md`.
