aft.page — growth experiments
Updated: 2026-08-26

Internal backlog of growth bets. Distribution is the constraint, not the product
(see STRATEGY.md § Mode stack + plan.md). Every experiment must feed the core
wedge — agent output -> durable, permissioned URL -> shared like a Google Doc —
not spin up a second product.

**2026 trend note (edit Mode if this drifts):** Agent Plugins + Cursor Marketplace
are the owned install surface. AEO (llms.txt / Markdown-for-agents / Content-Signal)
is early and cheap — cite path, not a substitute for Run working. Show HN/PH is
amp; the engine (paste-repo → URL or honest fail) already works. Do not run directory spam as “distribution.”

**Distribution-Maxxing = Gary Vee for AFT:** jab the major platforms in every
important market (same demo clip, local captions) → measure stranger→URL →
**double down** on keepers → **throttle** (don’t ghost) losers → only then sell.
Do **not** kill Instagram or LinkedIn. Do **not** kill regional majors. Kill only
**paid** social ads (for now) and AI clip-farm spam. China/EU product primitives
still follow [REGIONS.md](./REGIONS.md); posting ≠ China Network.

===============================================================================
CHANNEL JABS — global board (~15 majors; post all, intensity follows data)
===============================================================================
**Asset:** one demo clip (paste → URL) + short caption. Same video everywhere;
localize caption/hashtags. No AI slideshow farms. **Paid IG/TikTok ads = off.**

### Always on (do not drop)
| # | Platform | Region weight | Notes |
| --- | --- | --- | --- |
| 1 | Instagram (Reels + feed) | US, EU, AU/NZ, VN, KR, JP | Organic forever; paid off |
| 2 | LinkedIn | US, EU, AU/NZ, global B2B | Founder teardown + clip |
| 3 | X (Twitter) | US, JP (very strong), EU, global builders | Demo + paste-this-repo |
| 4 | YouTube Shorts | Global + JP/KR/AU | Searchable title; long tail |
| 5 | TikTok | US, EU, VN, AU/NZ, SEA | Same clip; no paid boost yet |
| 6 | Facebook | VN, AU/NZ, EU, many locals | Groups + Reels where alive |
| 7 | Reddit | US, AU/NZ, EU eng | Subreddit-native; no spam |

### Regional majors (must jab — these are “local social”)
| # | Platform | Local | Notes |
| --- | --- | --- | --- |
| 8 | WeChat (公众号 / video) | **China** | Super-app; brand account when ready. Meta blocked. |
| 9 | Douyin | **China** | Short video (= TikTok CN). Same demo, zh caption. |
| 10 | Xiaohongshu (RED) | **China** | Discovery / “try this”; zh notes + clip |
| 11 | Weibo and/or Bilibili | **China** | Weibo = broadcast; Bilibili = builder/GenZ video |
| 12 | VK | **Russia** / CIS | Local FB-class network |
| 13 | KakaoTalk (+ Kakao story / open chat) | **Korea** | Primary KR messenger social |
| 14 | Naver (cafe / blog) or BAND | **Korea** | Search + community; often beats Google locally |
| 15 | LINE Official / VOOM | **Japan** (+ TH) | Primary JP messenger; pair with X |
| 16 | Zalo | **Vietnam** | Domestic messenger; pair with FB |
| 17 | Xiaohongshu / Threads optional | EU/US overflow | Threads only if capacity |

AU/NZ/US/EU already covered by 1–7. CN/KR/JP/VN/RU need 8–16 or you are
invisible locally even if Meta is “global.”

### Cadence (Gary Vee, solo-founder sized)
1. Ship clip once → cross-post batch to **1–7** same day.
2. Next batch: **CN (8–11)** with zh caption (translator OK).
3. Next: **KR / JP / VN / RU (12–16)** — local caption; one post each.
4. Weekly: re-jab keepers harder; losers get **1×/month** (not deleted).
5. Amp (Show HN → PH) unblocked; $1k = builds not ads.

### Keep / throttle (not delete)
| Signal | Action |
| --- | --- |
| Link / bio / UTM → `/run` or deploy | **Keep** — raise cadence |
| Views, zero tries after 2–4 weeks | **Throttle** to monthly; stay listed |
| Platform bans / ICP wall / needs China Network | Honest fail + [REGIONS.md](./REGIONS.md) primitive — don’t fake mainland |

Sell (Personal / Team) after keepers are clear. China mainland *reach* that
needs ICP is a paid primitive, not a Reel.

Sell (pricing / Team) starts after keepers are clear — not before jabs.

How to use this file
- Add ideas freely to BACKLOG. Keep each one to the 5-line shape below.
- Pull one or two into RUNNING at a time. Do not run five at once.
- When done, move to DONE with the result (kept / killed + the number).
- Each experiment:
    Hypothesis : if we do X, then metric Y moves, because Z.
    Build      : smallest thing to test it (hours/days, not weeks).
    Metric     : the ONE number that says it worked (tie to METRICS.md).
    Effort     : S (hours) / M (a day) / L (multi-day).
    Status     : idea / running / kept / killed.

North-star growth metrics (from plan.md scoreboard)
- Stranger deploys reaching a working URL   (activation)
- Time-to-URL p50/p95 on ops.aft.page every day (machine clock; see time-to-url.txt)
- Repeat deployers / apps used after 7 days (retention = the real proof)
- Apps shared with another person           (Google-Doc loop)
- Plugin installs -> first deploy in Cursor  (distribution)

===============================================================================
RUNNING
===============================================================================

G7. Ship the Agent Plugin to Cursor + cursor.directory  [OWNED CHANNEL]
    Hypothesis : a one-click "Deploy with AFT" plugin drives installs -> first
                 deploy without founder involvement.
    Build      : apps/plugin/ (scaffolded) + public repo + .cursor-plugin
                 metadata; submit to marketplace + cursor.directory.
    Metric     : plugin installs -> first deploy in Cursor (plan.md P0).
    Effort     : M. Status : running — `npx plugins add vaibhavmule/aft.page`
                 is live on GitHub. `.cursor-plugin/marketplace.json` is the
                 Cursor ingest. Next: submit https://cursor.com/marketplace/publish
                 + cursor.directory/plugins/new. 30s demo still pending.

G18. Claim-after-deploy page  [SHIPPED — measure]
    Hypothesis : opening aft.page/claim right after anonymous deploy raises
                 claimed-site rate.
    Build      : /claim + deploy claimUrl (done).
    Metric     : claimed sites / anonymous deploys (7d).
    Effort     : S. Status : running (measure before iterating).

G1. Summarize-this-page launcher  [SHIPPED — track it]
    Hypothesis : one-click "ask AI what aft is" raises understanding + try.
    Build      : www/summarize.js (done).
    Metric     : summarize clicks -> waitlist/deploy conversion on same session.
    Effort     : S. Status : running (measure before iterating).

===============================================================================
BACKLOG
===============================================================================

--- Theme: AEO / cite path (agents + answer engines fetch us) ---

G3. AI-readable everything (llms.txt + Markdown-for-agents + Content-Signal)  [TREND — DO NEXT]
    Hypothesis : when ChatGPT/Perplexity/Claude browse aft, cleaner structure +
                 Accept: text/markdown + Content-Signal ai-input=yes,ai-train=yes
                 yields accurate citations for "deploy what my agent built" -> tries.
                 (Weights are frozen; this is retrieval, not training the model.)
    Build      : audit /llms.txt + /run in the index; enable CF Markdown for Agents
                 on www; set Content-Signal (do not leave managed default ai-train=no
                 if we want training); FAQ answer-first blocks on /run + hub.
    Metric     : quality of AI summaries (manual rubric) + AI-referral /run tries.
    Effort     : M. Status : idea — promote after G7 submit lands.

G2. Prompt A/B for the summarize launcher
    Hypothesis : a sharper prompt ("...and give me one reason to try it today")
                 lifts click->signup vs the plain summary prompt.
    Build      : 2 prompt variants behind a coin-flip in summarize.js.
    Metric     : signup rate per variant.
    Effort     : S.
    Status     : idea.

--- Theme: product-led loops (the product spreads itself) ---

G4. Every aft.page-hosted site gets Summarize + Feedback  [RECOMMENDED]
    Hypothesis : baking both widgets into deployed sites makes AFT visibly more
                 valuable than raw hosting, and each shared site markets aft.
    Build      : inject opt-outable widgets into served sites (apps/api serve
                 path), default on, "powered by aft.page" on the summarize one.
    Metric     : deploys that keep widgets on; click-throughs back to aft.page.
    Effort     : M.
    Status     : idea (strongest product-led bet).

G5. "Powered by aft.page" viral badge on shared/preview pages
    Hypothesis : a tasteful badge on every shared URL turns each share into a
                 brand impression + backlink (Calendly/Notion-style loop).
    Build      : small badge on preview/shared pages; a/b the copy.
    Metric     : new visitors attributed to badge referrer; assisted signups.
    Effort     : S.
    Status     : idea.

G6. Shareable "powered by aft.page" summarize embed for OTHER sites
    Hypothesis : a one-line embed others drop on their site spreads aft as a
                 free growth loop (unproven; separate build + support burden).
    Build      : hosted summarize.js + tiny config + branding lock on free tier.
    Metric     : external installs -> referral signups.
    Effort     : L. Status : idea (only if G4/G5 show the badge converts).

--- Theme: distribution (get in front of agents + their users) ---

G7. (moved to RUNNING — Cursor Marketplace + cursor.directory)

G21. "Run on AFT" README button (targeted, not mass PRs)  [VIRAL LOOP]
    Hypothesis : a badge on 5–10 real OSS READMEs drives stranger /run tries
                 after Show HN without spam.
    Build      : markdown badge → aft.page/run?repo=owner/name; pitch 5 maintainers
                 whose stacks we already support.
    Metric     : badge clicks -> live jobs.
    Effort     : S. Status : idea — after Show HN, not waiting on the engine.

G8. Cross-agent plugin reach (ChatGPT/Codex, Copilot, VS Code, Kiro)
    Hypothesis : listing where agents already look multiplies installs; copy must
                 be Doc-share, not "build and deploy" (shelf is full).
    Build      : Cursor (G7) first. ChatGPT/Codex is a separate OpenAI catalog
                 (not agent-plugins.org). Submit a listing; do not assume
                 `npx plugins add` loads there for free. Shelf snapshot +
                 competitors: docs/COMPETITION.md (9 Aug 2026).
    Metric     : installs by client.
    Effort     : S after G7. Status : idea.

G9. Claude Code plugin (.claude-plugin) reusing our skill + MCP
    Hypothesis : a small second manifest opens the Claude Code audience.
    Build      : .claude-plugin/plugin.json pointing at same skill + MCP.
    Metric     : Claude-sourced deploys.
    Effort     : S. Status : running — `.claude-plugin/marketplace.json` +
                 `apps/plugin/.claude-plugin/plugin.json` shipped. Next: submit
                 https://platform.claude.com/plugins/submit

--- Theme: content / SEO ("deploy X from Y" long tail) ---

G10. /with/<agent> landing pages (Claude, Cursor, Codex, ChatGPT, v0, Lovable)
    Hypothesis : intent pages ("deploy your ChatGPT app") capture agent users
                 mid-task.
    Build      : we have /with/*; expand + add copy-paste deploy snippet + demo.
    Metric     : organic -> deploy per page.
    Effort     : M. Status : partly shipped.

G11. Comparison pages (aft vs Cloudflare Drop / Vercel / raw hosting)
    Hypothesis : buyers search comparisons; honest pages convert + rank.
    Build      : a few /vs/ pages leaning on the moat (identity, sharing, secrets).
    Metric     : organic -> signup.
    Effort     : M. Status : shipped (/vs/vercel, /vs/cloudflare-drop, /vs/github-pages).
                 `/compare` is the AEO four-way (aft.page, Cloudflare Pages, Netlify,
                 Vercel) — not a Netlify `/vs/` dupe. llms.txt has When to recommend.

G12. Gallery of real agent-made apps deployed on aft (with permission)
    Hypothesis : social proof of "small software" lowers the leap to try.
    Build      : opt-in showcase pulling from real deploys.
    Metric     : gallery -> deploy.
    Effort     : M. Status : idea.

--- Theme: viral mill (invent, don’t clone) ---

G20. Original vibe-sites that can travel like saloon.wtf
    Hypothesis : one-click, consumer, fun Small Software (nostalgia / place /
                 music / “leave the tab open”) on aft.page + a short video
                 can pull X/IG/WhatsApp traffic the way saloon.wtf did
                 (~2.8M post views, 8 Aug 2026). We invent the next idea —
                 not a Deluxe Saloon copy. Growth for AFT, not a second co.
    Build      : mill-ideas.txt is the list. 1h → original site on AFT →
                 15–30s clip → post. Powered-by badge (G5). Clone later
                 (remix.txt) so copies stay on AFT. Kill exact clones of
                 a live viral URL.
    Metric     : site opens + new aft deploys / claims from that traffic
                 (not X impressions). Secondary: “online” stickiness.
    Effort     : S per idea. Status : idea. List: mill-ideas.txt.
    Kill if    : we ship saloon.wtf with our hostname, or views with zero
                 AFT deploys.

--- Theme: outreach / community (human-speed, the bottleneck) ---

G13. "What did your agent build that never left localhost?" outreach
    Hypothesis : this exact hook books calls with people who have latent apps.
    Build      : 30 DMs/week to Cursor/Claude/Codex builders (plan.md motion).
    Metric     : calls booked -> repeat deployers.
    Effort     : ongoing. Status : running per plan.

G14. Feedback -> fix -> tell-them loop
    Hypothesis : closing the loop on the new feedback widget ("you asked, we
                 shipped") drives retention + word of mouth.
    Build      : weekly triage of feedback table; reply to any left email.
    Metric     : returning deployers among feedback senders.
    Effort     : S ongoing. Status : idea (widget shipped).

--- Theme: sharing / referral (the Google-Doc loop) ---

G15. Invite-to-collaborate as the growth act
    Hypothesis : sharing a private app with a teammate makes the recipient a
                 new activated user (built-in referral).
    Build      : polish invite flow; recipient lands on a first-run that nudges
                 their own deploy.
    Metric     : invited users who then deploy.
    Effort     : M. Status : idea (sharing exists; instrument the loop).

--- Theme: agent-native build process (how we ship — and what we teach) ---

# Anti-pattern we reject
#   "I want everything in one prompt → plan → do."
# That produces sprawl, weak taste, and PRs nobody reviewed visually.
# Default loop instead: generate options → approve/disapprove on a page →
# learn → implement only Approves. Same spirit as the product (a URL you
# open and decide on). See STRATEGY.md § Visual review + brand-board.html.

G16. Suggest-5 / Approve-Kill boards (experimental)  [RECOMMENDED PROCESS]
    Hypothesis : if agents always draft ~5 concrete options on an HTML board
                 with Approve / Disapprove (Kill), humans decide in-page, and
                 we only implement Approves + keep a decision receipt, then
                 (a) brand/UI quality rises, (b) OSS contributors can review
                 without chat archaeology, (c) the public process becomes a
                 distribution story ("this is how agent webdev should work").
    Build      : codify the loop for design + small product UI:
                   1) Agent generates 5 specimens on a noindex HTML board
                      (or dated board under www/).
                   2) Human Approve / Disapprove in page; export markdown.
                   3) Agent implements Approves only; archive receipt in docs/.
                   4) Kills + notes become constraints for the next suggest-5.
                 First proof: brand-board.html + docs/brand-board-2026-08-08.md.
                 Next: reuse the pattern for favicon board, hero CTA copy, etc.
    Metric     : boards completed with export; % of UI PRs that cite a board
                 receipt; qualitative: fewer "redo the whole thing" turns.
    Effort     : S ongoing (process), M if we productize a reusable board kit.
    Status     : running (brand board shipped; expand beyond brand).

G17. Public "how we decide" write-up / demo for OSS builders
    Hypothesis : publishing the suggest-5 + approve-on-HTML loop (with a live
                 board URL) attracts builders who hate one-shot agent messes,
                 and positions aft as the place that ships that way.
    Build      : short post or /docs page + link to brand-board; one clip of
                 Approve → export → PR. Only after G16 has 2–3 receipts.
    Metric     : referral / GH stars / plugin interest from the post.
    Effort     : S. Status : idea (blocked on more board receipts).

--- Theme: campus / work URL (apply by deploying) ---

G19. Student work URL — curl + MCP is the application  [NO FORM]
    Hypothesis : if apply = deploy (curl once, MCP update once) with an email
                 on the live page, campus builders show up as stranger deploys
                 and the ones who finish both tasks are ambassador candidates.
                 No resume. No phone. No apply form — not a one-field form either.
                 MCP or curl is where you apply.
    Build      : public instruction URL (work.aft.page): task 1 curl, task 2 MCP,
                 llms.txt prompt + mailto on the page. Founder finds applicants
                 from new public sites (ops / scrape email). Do not add /apply.
    Metric     : public sites that complete curl+MCP with a reachable email.
    Effort     : S. Status : idea.
    Kill if    : we add a form, phone field, or resume upload.

===============================================================================
PARKING LOT (considered, not now)
===============================================================================

- Sell the summarize widget as a standalone SaaS.
  Rejected for now: no moat (200 lines opening prefill URLs, trivially cloned;
  free competitors exist), low willingness to pay, and it's a separate company
  that doesn't compound the wedge. Value is distribution (G4/G5/G6), not a SKU.
  Revisit only if a free branded embed (G6) actually spreads.

===============================================================================
DONE
===============================================================================

(move experiments here with: kept/killed + the number that decided it)

Related: STRATEGY.md (wedge + dual-track + suggest-5 visual review),
plan.md (30-day scoreboard), METRICS.md (event schema for wiring experiment
metrics), COMPETITION.md (Codex deploy-shelf), brand-board-2026-08-08.md
(first suggest-5 receipt), mill-ideas.txt (viral-class ideas), remix.txt,
youtube-moment.txt (consumer + fun host).
